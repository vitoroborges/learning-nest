version: '3.8'

services: 
  db:
    build: .
    container_name: learning_nest-db
    restart: always
    ports: 
      - '5432:5432'
    volumes:
      - .docker/dbdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=learning_nest
